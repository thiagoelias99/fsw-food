/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['pt-BR'],
    defaultLocale: 'pt-BR',
  },
  images:{
    domains: ['utfs.io', 'lh3.googleusercontent.com']
  },
}

export default nextConfig
